pasos:

terminal
npm init -y   //para iniciar node y packaje.json
npm install express mysql2 dotenv //para instalar el servidor http, el driver a la base de datos y el lector del .env(variables de entorno)

Por ahora uso CommonJS porque se me hace mas comodo pero si quiero usar ESM tengo que poner  "type": "module", en package(.json)

Poner comillas invertidas en los scripts-sql en js